## Database Configuration
#spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:4306}/${DB_NAME:skillswap_hub}?useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=${DB_USER:root}
#spring.datasource.password=${DB_PASSWORD:}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## JPA and Hibernate Configuration
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.properties.hibernate.format_sql=true
#
#app.jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
#app.jwt.expiration=${JWT_EXPIRATION:86400000}
#
## Server Configuration
#server.port=${PORT:8080}
#
## Logging
#logging.level.com.skillswap.skillswaphub=DEBUG
#logging.level.org.springframework.security=DEBUG
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=${JWT_EXPIRATION}

server.port=${PORT}

logging.level.com.skillswap.skillswaphub=DEBUG
logging.level.org.springframework.security=DEBUG
